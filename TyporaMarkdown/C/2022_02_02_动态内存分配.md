**malloc**

    void* malloc(size_t size);
    
    void* p = malloc(40);//字节
                ||
    int * p = (int *)malloc(40);

## 柔型数组

    结构体最后一个成员可以是未知大小
    未知成员前至少有一个成员
    sizeof返回值不包含柔性成员
    
    strucr st_type
    {
        int i;
        int arr[];//int arr[0];
    };
    
    sizeof(strucr st_type);//4
    
    创建变量配合malloc使用

